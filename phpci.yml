build_settings:
    ignore:
        - "vendor"
        - "locales"
        - "utils"
        - "sandbox"
        - "templates"
        - "public/static"

setup:
    composer:
        action: "install"
    
test:
    php_unit:
        config: 
            - "phpunit.xml"
    php_mess_detector:
        allowed_warnings: true
    php_code_sniffer:
        standard: "PSR2"
        allowed_errors: true
        allowed_warnings: true
    php_cpd:
        allow_failures: true
    lint:
    php_loc: